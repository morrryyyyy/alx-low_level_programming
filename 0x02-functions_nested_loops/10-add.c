#include "main.h"

/**
 * add - adds two integers and prints the result
 * @a: one integer
 * @b: the second integer
 *
 * Return: the result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
